# BISSECCAO passo 0: minimo absoluto. Se ate isto falhar, o problema nao e o
# projeto — e o ambiente de build do EasyPanel (pull de imagem, buildx, disco).
FROM node:22-bookworm-slim

RUN node -v && echo ok

CMD ["sleep", "infinity"]
